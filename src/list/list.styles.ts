import { css } from "lit-element";

export default css`
  .headline {
    display: flex;
    font-size: 12px;
    font-weight: 700;
    color: var(--theme-ui-invert);
    padding: 0 25px;
    line-height: 25px;
    height: 25px;
    background-color: rgba(236, 240, 241, 0.06);
  }
`;
